@using CrudPerson.BusinessLibrary.BusinessModel;
@using CrudPerson.WebUI.Controllers;
@model Person

@{
    const string PAGE_TITLE = "Details";
    ViewData["Title"] = PAGE_TITLE;
}

<h1>@PAGE_TITLE</h1>

@Html.DisplayForModel(nameof(Person))

<div>
    <a asp-action="@nameof(PersonController.Edit)" asp-route-identifier="@Model.Identifier">Edit</a> |
    <a asp-action="@nameof(PersonController.Delete)" asp-route-identifier="@Model.Identifier">Delete</a> |
    <a asp-action="@nameof(PersonController.Index)">Back to List</a>
</div>
